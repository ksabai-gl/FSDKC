{
  "name": "klearcom",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,cyan \"npm run dev --prefix dev-api\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && npm install --prefix dev-api && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
