// Deprecated duplicate test suite.
// The canonical tests now live alongside the consolidated component at
// src/components/DiscoveryJobs/__tests__/DiscoveryJobsTable.test.tsx
// (see PR #10 review comment from ksabai-gl). This file is intentionally
// left as a no-op placeholder to avoid duplicate/conflicting test
// coverage for the same component.
describe('DiscoveryJobsTable (deprecated location)', () => {
  it('is consolidated into src/components/DiscoveryJobs — see canonical test suite', () => {
    expect(true).toBe(true);
  });
});
